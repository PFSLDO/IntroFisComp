/*
 Calcular as seguintes funções, tendo atenção aos seus domínios:
*/

#include "stdio.h"
#include "stdlib.h"
#include "math.h"

//------------------------------------------------------------------------------------------------------------------------------

int main() {
  float num, eq1, eq2, eq3, eq4, eq5;

  printf("Digite um numero:\n");
  scanf("%f", &num);

  if (num != 0) {
    printf("Como o numero eh diferente de 0, podemos calcular todas as equacoes:\n");

    //eq1 = ;
  }
  else {
    printf("Como o numero eh 0, podemos calcular apenas as equacoes 2, 3, e 4:\n");
  }

  return 0;
}